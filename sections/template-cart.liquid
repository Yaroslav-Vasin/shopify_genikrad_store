{% if cart.item_count > 0 %}

{% else %}
  <div class="max-w-6xl h-60 my-4 flex items-center justify-center">
    <div class="text-center">
      <h1 class="text-3xl my-4">Your cart</h1>
      <div class="my-4 py-4">
        <a href="" class="border-gray-600"></a>
      </div>
    </div>
  </div>
{% endif %}